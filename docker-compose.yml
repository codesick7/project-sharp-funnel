services:
  mcp-funnel:
    build: .
    container_name: mcp-funnel-app
    restart: always
    init: true
    ports:
      - "6726:6726"
    environment:
      - PYTHONUNBUFFERED=1
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
